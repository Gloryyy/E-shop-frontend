import { Platform } from 'react-native';

let baseURL = 'https://easy-shop-server0.herokuapp.com/api/v1/';

/* let baseURL = '';

{
  Platform.OS == 'android'
    ? (baseURL =
        'http://6102-2a01-c23-752b-be00-3484-f495-ddc1-446b.ngrok.io/api/v1/') ngrok http 3000
    : (baseURL = 'http://localhost:3000/api/v1/');
} */

export default baseURL;
